{
	"name": "worker",
	"version": "0.0.1",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "doppler run -- bun run --watch src/index.ts"
	},
	"dependencies": {
		"bullmq": "^4.12.4",
		"db": "workspace:*",
		"nanoid": "^5.0.2",
		"zod": "^3.22.4"
	},

	"module": "src/index.js"
}
