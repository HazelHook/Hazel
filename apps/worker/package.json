{
	"name": "worker",
	"version": "0.0.1",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "doppler run -- bun run --watch src/index.ts"
	},
	"dependencies": {
		"bullmq": "^4.6.0",
		"db": "workspace:*",
		"nanoid": "^4.0.2",
		"zod": "^3.21.4"
	},

	"module": "src/index.js"
}
