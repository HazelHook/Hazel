NODE daily_res
SQL >

    SELECT
        toStartOfDay(received_at) AS bucket,
        customer_id,
        source_id,
        status,
        countState() AS count
    FROM response_events
    GROUP BY
        bucket,
        source_id,
        customer_id,
        status

TYPE materialized
DATASOURCE daily_res_mv


