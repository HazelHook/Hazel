NODE day_req
SQL >

    SELECT
        toStartOfDay(timestamp) AS bucket,
        customer_id,
        source_id,
        countState() AS count
    FROM request_events
    GROUP BY
        bucket,
        source_id,
        customer_id

TYPE materialized
DATASOURCE day_req_mv


